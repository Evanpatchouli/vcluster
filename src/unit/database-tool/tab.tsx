import {
  FolderPlus,
  Login as Import,
  ListAdd,
  PageTemplate,
} from "@icon-park/react";
import { FormattedMessage } from "react-intl";
import "./tab.css";

function Tab() {
  return (
    <div className="database-tool-tab">
      <p>Unsupported feature.</p>
    </div>
  );
}

export default Tab;
